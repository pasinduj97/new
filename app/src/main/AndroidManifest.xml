<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.madproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SucessActivity"></activity>
        <activity android:name=".QuestionActivity"></activity>
        <activity android:name=".Answer" />
        <activity android:name=".ManageReference" />
        <activity android:name=".ManageQuestion" />
        <activity android:name=".AdminDashboard" />
        <activity android:name=".SetsActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".UserDashboard"></activity>
        <activity android:name=".ManageSets" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Login"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".Register"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
    </application>

</manifest>